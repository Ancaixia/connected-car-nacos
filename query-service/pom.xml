<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>connected-car-demo</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>query-service</artifactId>
    <name>query-service</name>
    <description>查询/Web 服务：前端 API + 登录会话 + OpenFeign 调采集服务，并托管静态大屏</description>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>common</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <!--
          OpenFeign 按服务名调用时必须有负载均衡器。Spring Cloud 2020 起 Ribbon 被移除，
          nacos-discovery 只是 optional 依赖它，不会传递进来，必须显式声明，
          否则启动报：No Feign Client for loadBalancing defined.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.example.connectedcar.query.QueryApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
